from setuptools import find_packages
from setuptools import setup

setup(name='meld',
      version='0.1',
      description='Algorithm for meta-learning from images',
      url='todo',
      author='todo',
      author_email='todo',
      packages=find_packages(),
      zip_safe=False)
